name: studybuddy
description: AI StudyBuddy for Class 12 NCERT
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  google_generative_ai: ^0.4.6
  image_picker: ^1.1.2
  google_mlkit_text_recognition: ^0.13.0
  flutter_markdown: ^0.7.3
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
